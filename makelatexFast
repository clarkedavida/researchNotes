#!/bin/bash

# Single pdflatex pass. Does not rebuild bibliography or index.
# Use when iterating on prose; cross-references may show ?? if new.

pdflatex -interaction=batchmode -shell-escape 0_researchnotes.tex 2>&1 | grep -Ev "feynmf"
